# D3 / D5 HTTP bridge + first-mission oracle

Independent of Claude D1.2, the frozen 95-gate catalog, and the D2/D4 49-test pack.

```powershell
cd C:\AION-HQ-grok-director-d3-d5-oracle
node scripts/acceptance/director/regressions-d3-d5/run-d3-d5-oracles.mjs
```

Does not bind production 31415/443. Live HTTP uses ephemeral `127.0.0.1` ports.
Does not spend. Does not write Claude's worktree.
