# R6.5 Implementation Notes (tracked)

Status: IMPLEMENTED code; **NOT ACTIVATED** for real Owner approval root.
Founder authorization remains authoritative.

## Package

`packages/delegated-operator` (`@aion/delegated-operator`)

## Activation

`activationMode: "inactive"` is the production default path.
`synthetic_test` is for automated tests only.
`UnprovisionedRealOwnerPresence` always refuses until a later activation milestone.
No approval root is provisioned on server startup.

## Owner-presence selection

**Selected eventual path:** fixed elevated consent helper that HMAC-signs only a Host-built
challenge `aion-owner-approve-v1|<envelopeDigest>|<utc>|<presenceId>` under secure-desktop UAC,
with DPAPI-protected key material for the interactive Owner user.

**R6.5 ships:** synthetic HMAC adapter for tests + unprovisioned real adapter (fail-closed).

**Why not activated:** CURRENT forbids real approval-root provisioning in R6.5.

## Isolation from chat/model path

Normal AION `local-assistant` service/chat does **not** import this package.
Operation classes are closed; unknown ops refuse. No eval / public bind / remote shell.

## Known limitations

- Git ancestry for ordinary-forward HEAD is not fully validated inside the pure library (Host may extend).
- Real Windows UAC helper is designed but not provisioned.
- Mediated ops in tests return effect tokens; production process runners can be added later under same policy.
